syntax = "proto3";

option java_package = "com.fal.challengechapter6";
option java_multiple_files = true;

message UserProto {
  // filter for showing / hiding completed tasks
  string nama = 1;
  string userId = 2;
}